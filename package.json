{
  "name": "lemon-agent",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Lemon Agent — free tools, honest measurements, agentic services. Static site + canonical failure lists.",
  "scripts": {
    "sync": "mkdir -p public/lists && cp lists/*.md public/lists/",
    "predev": "npm run sync",
    "prebuild": "npm run sync",
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^5.12.0"
  }
}
